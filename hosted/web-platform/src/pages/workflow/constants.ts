/** Shared layout constants for workflow SVG nodes. */

export const NODE_W = 180;
export const NODE_H = 80;
export const NODE_R = 8;

/** Fallback colors for unknown node types. */
export const FALLBACK_COLOR = { fill: "#1a1a1a", header: "#666", stroke: "#666" };
