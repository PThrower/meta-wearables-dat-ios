{
  "name": "caringmind-frame-relay",
  "version": "0.2.0",
  "description": "WebSocket frame relay server with WASM frame processing and session mp4 export",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build:wasm": "cd ../packages/frame-relay-wasm && CARGO_TARGET_DIR=/tmp/frame-relay-target cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target bundler --out-dir ../server/pkg /tmp/frame-relay-target/wasm32-unknown-unknown/release/frame_relay_wasm.wasm",
    "start": "bun run src/server.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "test": "bun test"
  },
  "keywords": ["relay", "wasm", "streaming", "caringmind"],
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1024.0",
    "@aws-sdk/s3-request-presigner": "^3.1024.0",
    "@ebowwa/object-store": "^0.2.0",
    "@ebowwa/palantir": "^0.1.1",
    "@ebowwa/relay-protocol": "file:../packages/relay-protocol",
    "@google/genai": "^1.0.0",
    "drizzle-orm": "^0.45.2",
    "google-auth-library": "^9.0.0",
    "opusscript": "^0.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "drizzle-kit": "^0.31.10"
  },
  "systemRequirements": {
    "ffmpeg": ">=6.0 (required for session mp4 export)"
  }
}
