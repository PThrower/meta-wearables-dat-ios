# Bandwidth Offloading Architecture: Alternatives to the Relay Pattern

This repository, `docs/infrastructure-capacity-planning.md` establishes that outbound bandwidth ( not CPU/RAM is is the binding constraint at ~100 publisherss with 3 viewers each. a1 Gbps port saturates before CPU does. This doc analyzes the architectural alternatives to shifting that bandwidth burden away clients peers, examines trade-offs of each approach, and provides a decision framework for when each is worth adopting.

\n\nNow let me write the file. and create it. Add a a staleness warning section cross-rerefsencing to related docs.

 as needed.

 to understand the full context. when reading each of these docs.

---

## Staleness Warning
