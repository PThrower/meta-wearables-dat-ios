{
  "name": "caringmind-frame-relay",
  "version": "0.1.0",
  "description": "WebSocket frame relay server with WASM frame processing",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build:wasm": "cd ../crate && CARGO_TARGET_DIR=/tmp/frame-relay-target cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target bundler --out-dir ../server/pkg /tmp/frame-relay-target/wasm32-unknown-unknown/release/frame_relay_wasm.wasm",
    "start": "bun run src/server.ts"
  },
  "keywords": ["relay", "wasm", "streaming", "caringmind"],
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1024.0",
    "@aws-sdk/s3-request-presigner": "^3.1024.0",
    "@ebowwa/object-store": "file:../packages/object-store"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
